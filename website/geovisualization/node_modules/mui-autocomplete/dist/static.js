"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var useStaticCall = function useStaticCall(setAction, setError, setItems, setIsOpen, setValue) {
  //setData: rendering listed(json) data given by users.
  function staticCall(items, val, title) {
    setAction('loading');
    var data = items.filter(function (d) {
      return searchData(d[title], val);
    });

    if (data.length > 0) {
      setIsOpen(true);
      setAction('complete');
      return setItems(data);
    }

    setIsOpen(false);
    return setAction('search');
  } //searchData: Finding the match with typed value.


  function searchData(item, val) {
    return item.toLowerCase().includes(val);
  } //setting the default value from default data


  function dataFindbyId(id, items, title) {
    var a = items.find(function (item) {
      return item.id === id;
    });

    if (a) {
      setValue(a[title]);
      setAction('complete');
    } else {
      return true;
    }
  }

  return {
    staticCall: staticCall,
    dataFindbyId: dataFindbyId
  };
};

var _default = useStaticCall;
exports["default"] = _default;